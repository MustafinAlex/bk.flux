apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: cluster-config
  namespace: flux-system
spec:
  interval: 0m0s
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./oxygen
  prune: true
  wait: true
